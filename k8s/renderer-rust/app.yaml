apiVersion: v1
kind: Service
metadata:
  name: renderer-rust
  labels:
    app: renderer-rust
spec:
  ports:
  - port: 50051
  selector:
    app: renderer-rust
    tier: app
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: renderer-rust
  labels:
    app: renderer-rust
spec:
  selector:
    matchLabels:
      app: renderer-rust
      tier: app
  template:
    metadata:
      labels:
        app: renderer-rust
        tier: app
    spec:
      containers:
      - image: hatena-intern-2025-renderer-rust
        imagePullPolicy: Never
        name: renderer-rust
        env:
        - name: GRPC_PORT
          value: "50051"
        - name: RUST_LOG
          value: "info"
        envFrom:
        - configMapRef:
            name: renderer-rust-app-env-vars
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
          limits:
            cpu: 200m
            memory: 200Mi
        ports:
        - containerPort: 50051
          name: renderer-rust
        readinessProbe:
          exec:
            command: ["/bin/grpc_health_probe", "-addr=:50051", "-connect-timeout=5s", "-rpc-timeout=5s"]
          initialDelaySeconds: 5
        livenessProbe:
          exec:
            command: ["/bin/grpc_health_probe", "-addr=:50051", "-connect-timeout=5s", "-rpc-timeout=5s"]
          initialDelaySeconds: 10
        securityContext:
          runAsNonRoot: true
          capabilities:
            drop:
            - all
            add:
            - NET_BIND_SERVICE
          readOnlyRootFilesystem: true
